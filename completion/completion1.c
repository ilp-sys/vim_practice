#include <stdio.h>

int add_two_int(int a, int b)
{
	return (a + b);
}

float add_two_float(float a, float b)
{
	return (a + b);
}

int main()
{
	int	a = 42;
	int	b = 24;

	//C + x == special mode of insert mode, completion mode
	// C + x, C + n
	int sum = add_;

	return (0);
}
